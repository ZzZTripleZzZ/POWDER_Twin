# dt_sync — Python dependencies
# Install: pip install -r requirements.txt
# Or with conda: conda run -n dt_sync pip install -r requirements.txt

# Core
# Keep NumPy on the 1.x ABI until the local matplotlib stack is rebuilt for
# NumPy 2.x; the E2/E3 mock plotting path otherwise emits binary-ABI tracebacks.
numpy>=1.24,<2
scipy>=1.11

# Networking / ZMQ
pyzmq>=25.0
asyncssh>=2.14

# Protobuf (must match generated _pb2.py files — generated with 6.x)
protobuf>=6.0
grpcio-tools>=1.56

# ML
torch>=2.1
gymnasium>=0.29

# RL training
# stable-baselines3>=2.1   # optional: if using SB3 instead of custom PPO

# Plotting
matplotlib>=3.7

# Jinja2 (for docker-compose.replica.yaml.j2)
jinja2>=3.1

# POWDER / geni-lib (install separately via pip install geni-lib)
# geni-lib>=0.9

# Optional: LSTM CIR model training
# pandas>=2.0              # useful for log parsing
